{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "coreclr",
      "request": "launch",
      "name": "Launch .NET Web API",
      "program": "${workspaceFolder}/Example_${input:project}/Example_${input:project}.Api/bin/Debug/net9.0/Example_${input:project}.Api.dll",
      "args": [],
      "cwd": "${workspaceFolder}/Example_${input:project}/Example_${input:project}.Api",
      "stopAtEntry": false,
      "preLaunchTask": "build ${input:project}"
    }
  ],
  "inputs": [
    {
      "id": "project",
      "type": "pickString",
      "description": "Select the API project to debug",
      "options": ["FastEndpoints", "Mediator"]
    }
  ]
}
